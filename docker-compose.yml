version: '3.8'

services:
  packing-service:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./stowage.db:/app/stowage.db
    environment:
      - DATABASE_URL=sqlite:///./stowage.db
    restart: unless-stopped
